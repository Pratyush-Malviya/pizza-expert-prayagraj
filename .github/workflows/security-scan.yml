name: Security Scan

on:
  push:
    branches: [ "master", "main", "security-hardening" ]
  pull_request:
    branches: [ "master", "main" ]

jobs:
  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run npm audit
        run: npm audit --audit-level=high || true

      - name: Run linter
        run: npm run lint
